# فحص النسخة

فحوص الواجهة التالية أُجريت على النسخة التجريبية الأولى قبل تخصيص أسماء الربع.

- اختُبرت الواجهة على عرض ١٤٤٠ بكسل و٣٩٠ بكسل، وجدول الربع داخل عرض الجوال دون تمرير أفقي للصفحة.
- اختُبر تسجيل دفعة جزئية «١٢٥٫٥٠» بالأرقام العربية، وتحديث المحصّل والمتبقي والصندوق.
- اختُبر مصروف مدفوع ٦٠٫٢٥ ريال، وفصل المصاريف المدفوعة عن الالتزامات غير المدفوعة.
- اختُبر فتح شهر جديد مع نسخ الأسماء وصفْر الدفعات والمصاريف، مع بقاء الشهر السابق مستقلًا.
- اختُبرت أداة WebMCP للقراءة بإدخال صحيح وآخر غير صالح. لا توجد أخطاء في سجل المتصفح أثناء هذه التجارب.
- نجحت خمسة اختبارات لحساب الهللات، وفصل الالتزامات، وعدم إظهار اكتمال القطّة قبل آخر هللة، والتحقق من النسخ المستوردة والتواريخ، ومعالجة النصوص بأمان.
- نُفذ SQL فعليًا باستخدام PostgreSQL محلي عبر PGlite: قراءة الزائر، منع كتابة الزائر، منع الحساب غير المسؤول من التعديل وترقية نفسه، حفظ المسؤول، رفض التعديل المتعارض، رفض البيانات غير الصحيحة دون تغيير السجل، وعدم مسح البيانات عند إعادة تشغيل الإعداد.

بتاريخ ١٨ سبتمبر ٢٠٢٦ تم تنفيذ إعداد SQL في مشروع Supabase الفعلي وربط config.js بمفتاح Publishable العلني. نجحت القراءة عبر REST (HTTP 200)، وأُكد وجود ١٢ عضوًا وقطّة ٢٠٠ ريال للشخص وحمدان أمينًا، والمدفوعات كلها صفر. رُفضت محاولتا تحديث مجهول عبر REST وsave_qatta (HTTP 401)، وأُعيدت قراءة السجل للتأكد من عدم تغيّره. نجحت الاختبارات الخمسة المحلية بعد الربط. أُنشئ حساب المسؤول ومنحت له صلاحية qatta_admins، وتأكد تأكيد البريد وصلاحية المسؤول عبر SQL. نجحت معاينة الواجهة متصلة بالسجل الحقيقي: ظهرت حالة «سجل مشترك»، والـ١٢ عضوًا، وإجمالي ٢٬٤٠٠ ريال مع إخفاء أدوات التعديل للزائر. أُوقف التسجيل العام للحسابات الجديدة. نجح نشر GitHub Pages عبر Actions، وطابقت ملفات الواجهة والخطوط المنشورة النسخة المحلية بايتًا ببايت. أظهر الرابط العام السجل الحقيقي و١٢ عضوًا وإجمالي ٢٬٤٠٠ ريال. نُفذ اختبار حفظ بصلاحية المسؤول داخل معاملة تراجع عنها SQL، وبقي رقم المراجعة ١ دون تغيير بيانات القطّة. اختبار تسجيل الدخول من واجهة الموقع ينتظر إكمال المستخدم له. تتضمن الملفات أسماء الربع وألقابهم؛ لا توجد كلمات مرور في ملفات الموقع.

تم التحقق من تخصيص القائمة: ١٢ اسمًا بالألقاب وبالترتيب المطلوب، و٢٠٠ ريال للشخص، وحمدان (أبو ذنب) أمينًا للقطّة، وهدف شهري ٢٬٤٠٠ ريال. لا توجد دفعات أو مصاريف مسبقة. نجحت اختبارات الحسابات الحالية بعد التحديث.

## تحقق بعد الربط

1. افتح الرابط في نافذة خاصة وتأكد من ظهور الربع الـ١٢ بمدفوعات صفرية وأن أزرار التعديل مخفية.
2. ادخل بحساب المسؤول وتأكد من إتاحة أدوات التعديل. يمكنك اختبار تسجيل دفعة ثم إرجاعها للصفر قبل الاستخدام الحقيقي.
3. تأكد من ظهور الدفعة في النافذة الخاصة بعد التحديث، وبقائها بعد إغلاق وإعادة فتح المتصفح.
4. أزل أي بيانات اختبار قبل بدء التسجيل الفعلي.

## إعادة الاختبارات محليًا

```bash
node tests/ledger.test.cjs
npm install --prefix /tmp/qatta-db-tests @electric-sql/pglite@0.3.7 --no-audit --no-fund
QATTA_PGLITE_MODULE=/tmp/qatta-db-tests/node_modules/@electric-sql/pglite/dist/index.js node tests/database.test.mjs
```

## دخول باسم المستخدم

أضيف دعم أسماء الدخول للحسابات المستقلة (مثل hamdan)، مع الحفاظ على دخول الحسابات الحالية بالبريد. نجحت الاختبارات المحلية الستة، بما فيها تحويل اسم الدخول وتنظيف المسافات ورفض الأسماء غير الصالحة. لا يمنح الاسم صلاحية إلا بعد إنشاء المستخدم وإضافته إلى qatta_admins. اختبار الدخول الفعلي لحساب حمدان ينتظر إنشاء كلمة مروره بواسطة مالك المشروع.
